<h2 for="totalAmount" class="col-3 text-end">Total:</h2>
<div class="col-5" style="font-size: 50px;color:#e3885b">{{totalAmount}}</div>
<ul class="list-group">
    {{#each records}}
        <li class="list-group-item">
            <div class="row">
                <div class="col-auto mr-auto">

                    <h4>{{this.name}}</h4>
                    <small>{{this.date}}</small>
                </div>
                <div class="col-auto amount mr-4">${{this.amount}}</div>

                <a ref="./records/{{this._id}}/edit">
                    <button class="btn btn-success" >Edit</button>
                </a>
                <form action="/records/{{this._id}}?_method=DELETE" method="POST" style="display: inline;"
                      onsubmit="return confirm('Are you sure want to delete {{this.name}} ?');">
                    <button class="btn btn-danger" type="submit">Delete</button>
                </form>
                </div>
        </li>
    {{/each}}
</ul>
<a href="./records/new">
    <button class="btn btn-primary btn-sm">Create</button>
</a>

